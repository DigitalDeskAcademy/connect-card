/**
 * Settings Page Header
 *
 * Settings pages typically use their own internal navigation structure,
 * so we opt out of the standard page header pattern.
 */
export default function SettingsHeader() {
  return null;
}
