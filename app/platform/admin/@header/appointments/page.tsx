import { PageHeader } from "@/components/layout/page-header";

/**
 * Platform Appointments Page Header
 *
 * Displays page title and appointment filters.
 * Server component - renders on initial page load.
 */
export default function AppointmentsHeader() {
  return <PageHeader title="Calendar" />;
}
