import { PageHeader } from "@/components/layout/page-header";

/**
 * Platform Dashboard Header
 *
 * Displays page title for platform admin dashboard.
 * Server component - renders on initial page load.
 */
export default function PlatformDashboardHeader() {
  return <PageHeader title="Platform Dashboard" />;
}
